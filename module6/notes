При запуске docker compose up -d --scale app=3

Так как в конфигурации контейнера указан один порт, то все последующие контейнеры-экземпляры
этого сервиса будут пытаться забиндить этот же порт -> что, конечно, вызывает ошибку
